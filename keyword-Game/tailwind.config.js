/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./*.html"], // Scan all HTML files in the root directory
  theme: {
    extend: {}, // Extend default Tailwind theme here if needed
  },
  plugins: [],
};
